import React from 'react';

const SectionTitle = ({heading}) => {
    return (
        <div >
            <h1 className='text-[25px]  my-4  font-bold'>{heading}</h1>
        </div>
    );
};

export default SectionTitle;